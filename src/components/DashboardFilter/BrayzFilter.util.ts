// TODO: import all functions to a util

export const handleSelectAll = (filterType: any) => {
  console.log(
    'Handle the section checkbox selection, passing through prop the option.title "Operatos" to handleSelectAll '
  );
};
